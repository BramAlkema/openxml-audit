"""Scenario generators for the Word roundtrip oracle.

Each scenario module produces a sequence of `ScenarioSpec` instances,
which the driver in `tools/oracle/word_repair_oracle.py` materializes
as DOCX files and roundtrips through Word.

Spec: `specs/011-word-roundtrip-oracle.md` Phase 2.
"""
